html
  overflow-x hidden

*,
*::before,
*::after
  box-sizing border-box

.page
  margin-top vw(132)
  font-family $font-family
  font-size vw(16)
  font-weight 400
  overflow-x hidden
  +min-max-w(1px, $tab)
    margin-top vwm(86)
    font-size vwm(16)